numbers=(1,2,3,5)
strings=('nico','zule','samy')
print(numbers)

print('o',numbers[0]) # mismas llaves para las tuplas

print(type(numbers))

# en las listas tenemos crud

#en la dupla solo se puede declarar

#buscar elemntos de la tupla

print(strings.index('zule'))
print(strings.index('samy'))

my_list=list(strings) # se cambia a lista con esta funcion

my_tuple=tuple(my_list) # pasa de lista a tupla